apiVersion: v1
kind: Service
metadata:
  labels:
    app: pagamento
  name: pagamento
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app: pagamento
  type: ClusterIP
